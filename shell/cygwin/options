if [[ "$OSTYPE" == "cygwin" ]]; then

if [ -z "$HTTP_PROXY" ]; then
    echo "Enviroment variable HTTP_PROXY doesnt exist. See ~/.sensitive"
fi

# ZSH
plugins=(cp autojump svn zsh-syntax-highlighting)

# GLOBAL
export SVN_EDITOR="vim"
export NO_PROXY=localhost,127.0.0.0/8,127.0.1.1,10.1.0.122
export BROWSER="/cygdrive/c/Documents and Settings/lcpereira/Configurações locais/Dados de aplicativos/Google/Chrome/Application/chrome.exe"

fi
